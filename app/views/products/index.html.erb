<div class="container">
<div class="row">
	<div class="col-xs-2">
		<%= render 'genres/index', genres: @genres %>
	</div>
	<div class="col-xs-10">
		<h2>商品一覧全<%= Product.all.count %>件</h2>
		<% @products.each do |product| %>
		<%= render 'products/index', product: product %>
		<% end %>
	</div>
</div>
</div>

